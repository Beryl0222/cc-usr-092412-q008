{
  "name": "clinician-honor-contribution",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "领域事件基础校验",
  "scripts": {
    "test": "node --test",
    "build": "node --check src/validator.js && node --check src/honor-case.js && node --check scripts/build-sample.mjs",
    "build:sample": "node scripts/build-sample.mjs"
  }
}
